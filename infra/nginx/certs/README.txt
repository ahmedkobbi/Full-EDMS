# Place TLS certs here:
#   fullchain.pem
#   privkey.pem
# Generated via:
#   openssl req -x509 -newkey rsa:4096 -nodes -keyout privkey.pem -out fullchain.pem -days 365 -subj "/CN=smart-edms.local"
#
# For production: use a real CA (Let's Encrypt, internal CA, etc.)
