/**
 * @smart-edms/i18n — fr translation: `tour.search` namespace.
 *
 * Source of truth: en/tour/search.ts
 * Translated from the English baseline.
 */

const search = {
  title: 'Search tour',  // falls back to English
  subtitle: 'Find any document in seconds.',  // falls back to English
  'step.intro.title': 'Search smarter',  // falls back to English
  'step.intro.body': 'This tour shows you how to find anything — by name, content, metadata, or tags.',  // falls back to English
  'step.searchbar.title': 'The search bar',  // falls back to English
  'step.searchbar.body': 'Start typing here to search across document names, content (including OCR text), and metadata. Results update as you type.',  // falls back to English
  'step.filters.title': 'Filters',  // falls back to English
  'step.filters.body': 'Use the filters panel to narrow by file type, classification, tag, owner, or date range.',  // falls back to English
  'step.advanced.title': 'Advanced search',  // falls back to English
  'step.advanced.body': 'For complex queries, open the advanced search builder. You can compose multiple conditions and group them with AND / OR.',  // falls back to English
  'step.fullText.title': 'Full-text search',  // falls back to English
  'step.fullText.body': 'Full-text search finds documents containing your terms anywhere in the content. Highlights show exactly where the match is.',  // falls back to English
  'step.saved.title': 'Saved searches',  // falls back to English
  'step.saved.body': 'Run the same search often? Save it. You can even schedule a saved search to run daily and email you the results.',  // falls back to English
  'step.recent.title': 'Recent searches',  // falls back to English
  'step.recent.body': 'Your recent searches appear here so you can quickly rerun something you just did.',  // falls back to English
  'step.facets.title': 'Facets',  // falls back to English
  'step.facets.body': 'The facets sidebar shows you the breakdown of results by type, classification, owner, and more. Click a facet to refine.',  // falls back to English
  'step.sort.title': 'Sorting',  // falls back to English
  'step.sort.body': 'Sort results by relevance, date, name, or size. Relevance uses a full-text ranking algorithm.',  // falls back to English
  'step.flex.title': 'Flex search',  // falls back to English
  'step.flex.body': 'Flex search lets you search across multiple tenants or document stores at once — useful for investigations and audits.',  // falls back to English
  'completion.title': 'You’re a search expert',  // falls back to English
  'completion.body': 'You can now find anything in Smart EDMS. Take the Audit tour to learn about the tamper-evident log.',  // falls back to English
  'completion.next': 'Take the Audit tour',  // falls back to English
} as const;

export default search;
